import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Walmart Demand Forecasting - AI-Powered Inventory Management',
  description: 'Revolutionary hyper-local demand intelligence that eliminates stockouts, reduces waste by 85%, and optimizes inventory with 94% accuracy.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}